DECLARE @p int = 2;

SELECT [t0].[Id], CAST(ROW_NUMBER() OVER(ORDER BY [t0].[Id]) - 1 AS int) AS [Index]
FROM (
    SELECT [t].[Id]
    FROM [TestRows] AS [t]
    ORDER BY [t].[Id]
    OFFSET @p ROWS
) AS [t0]
ORDER BY [t0].[Id]