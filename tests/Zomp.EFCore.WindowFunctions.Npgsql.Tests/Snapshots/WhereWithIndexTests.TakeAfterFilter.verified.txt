-- @p='2'
SELECT t0."Id"
FROM (
    SELECT t."Id", CAST(ROW_NUMBER() OVER(ORDER BY t."Id") - 1 AS integer) AS "Index"
    FROM "TestRows" AS t
) AS t0
WHERE t0."Index" % 2 = 1
ORDER BY t0."Id"
LIMIT @p