SELECT t0."Id", t0."Col1"
FROM (
    SELECT t."Id", t."Col1", CAST(ROW_NUMBER() OVER(ORDER BY t."Id" DESC) - 1 AS integer) AS "Index"
    FROM "TestRows" AS t
) AS t0
WHERE t0."Index" % 3 = 0
ORDER BY t0."Id" DESC