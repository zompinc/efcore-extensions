SELECT t."Id", (
    SELECT count(*)::int
    FROM "TestRows" AS t0
    WHERE t0."Id" / 10 <= t."Id" / 10) AS "UpTo", (
    SELECT count(*)::int
    FROM "TestRows" AS t1
    WHERE t."Id" / 10 <= t1."Id" / 10) AS "From"
FROM "TestRows" AS t
ORDER BY t."Id"