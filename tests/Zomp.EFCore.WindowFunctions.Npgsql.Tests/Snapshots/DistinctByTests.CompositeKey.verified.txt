SELECT t0."Id"
FROM (
    SELECT t."Id", CAST(ROW_NUMBER() OVER(PARTITION BY t."Id" / 10, t."Id" % 2 ORDER BY t."Id") - 1 AS integer) AS "Index"
    FROM "TestRows" AS t
) AS t0
WHERE t0."Index" = 0
ORDER BY t0."Id"