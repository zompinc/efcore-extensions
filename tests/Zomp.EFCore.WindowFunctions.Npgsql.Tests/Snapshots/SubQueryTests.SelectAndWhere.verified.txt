SELECT t0."Id", t0."Col1", t0."Date", t0."IdBytes", t0."SomeGuid", t0."RowNumber"
FROM (
    SELECT t."Id", t."Col1", t."Date", t."IdBytes", t."SomeGuid", ROW_NUMBER() OVER(ORDER BY t."Id") AS "RowNumber"
    FROM "TestRows" AS t
) AS t0
WHERE t0."Id" < 10 AND t0."RowNumber" = 1