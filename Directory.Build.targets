<Project>
  <Target Name="HackVersion" AfterTargets="GetBuildVersion" Condition="'$(OverridePackageVersion)'!=''">
    <PropertyGroup>
      <PackageVersion>$(OverridePackageVersion)</PackageVersion>
    </PropertyGroup>
  </Target>
</Project>
